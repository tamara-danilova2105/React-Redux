// CartsItems - массив, который создается из элементов, на которые была кликнута кнопка "в корзину"
// у элементы в массиве параметр dishId === id элемента из dishesData
// dishes - элемент который найден методом gind dishesData по совпадению его id с dishId
// условие в функции, которая добавляет товар в корзину - если есть то, добавляей увеличивай кол-во, если нет добавляй в массив:

if (cartItems.some(cartItem => cartItem.dishId === dishes.id)) {
    cartItems.map(cartItem => {
        if(cartItem.dishId === dishes.id) {
            cartItem.quantity + cartItem.quantity // наверное что-то другое - cartItem.quantity ++ по 1шт добалять
        }
        return cartItem
    })
}
else { 
    cartItems.push({
        id: timeId,
        dishId: action.payload.dish.id,
        quantity: action.payload.quantity,
        totalPrice: action.payload.quantity * action.payload.dish.price,
    })
}
